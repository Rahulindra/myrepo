3344822053	dwh_sr_v2	10.253.195.133:59820	175	multichannel	select 
\nCONVERT ( date_format(  DATE_ADD(NOW(), INTERVAL 330 MINUTE)  , '%k%i' ),UNSIGNED INTEGER) hhmm ,  
\nDATE_SUB(CURDATE(), INTERVAL 1 DAY)  date_s , 
\ncase when registration_device =3 then 'ANDROID' when registration_device =4 then 'IOS' end as 
\nregistration_device,
\nsum(if(  date(signup_date ) =  DATE_SUB(CURDATE(), INTERVAL 1 DAY) and date_format(signup_date,'%k%i')  <= 
\nCONVERT ( date_format(  DATE_ADD(NOW(), INTERVAL 330 MINUTE), '%k%i' ),UNSIGNED INTEGER)  ,1,0))  as signup_company,
\nsum(if(  
\n   date_format( ftr_date ,'%k%i')  <= CONVERT ( date_format(  DATE_ADD(NOW(), 
\n   \tINTERVAL 330 MINUTE)  , '%k%i' ),UNSIGNED INTEGER)  and   date(ftr_date)  
\n   \t = DATE_SUB(CURDATE(), INTERVAL 1 DAY)  and  date(signup_date ) =  
\n   \t DATE_SUB(CURDATE() , INTERVAL 1 DAY)   , 1,0)) ftr_same_day ,
\nDATE_SUB(CURDATE(), INTERVAL 0 DAY)  date_s ,
\ncase when registration_device =3 then 'ANDROID' when registration_device =4 then 'IOS' end as 
\nregistration_device,
\nsum(if(  date(signup_date ) =
\n  DATE_SUB(CURDATE(), INTERVAL 0 DAY)   and date_format(signup_date,'%k%i')  
\n  <= CONVERT ( date_format(  DATE_ADD(NOW(), INTERVAL 330 MINUTE)  , '%k%i' 
\n  \t),UNSIGNED INTEGER)  ,1,0))  signup_company ,
\nsum(if(  date_format( ftr_date ,'%k%i') 
\n  \t <= CONVERT ( date_format(  DATE_ADD(NOW(), INTERVAL 330 MINUTE)  , '%k%i' ),UNSIGNED INTEGER)  and 
\n  \t   date(ftr_date)   = DATE_SUB(CURDATE(), INTERVAL 0 DAY)  and  date(signup_date ) = 
\n  \t    DATE_SUB(CURDATE() , INTERVAL 0 DAY)   , 1,0)) ftr_same_day 
\n
\nfrom
\n(
\nselect id  signup_company, created_at signup_date, s.company_id  ftr_company,registration_device,ftr_date
\n from companies  c join
\n(select company_id ,registration_device from users where registration_device in (3,4) and 
\n\tregistration_device is not null group by 1 ) as u on c.id=u.company_id
\n
\nleft join 
\n(select company_id,min(created_at) ftr_date from ship_credit_logs where status=1 and type=1 group by 1) s on c.id=s.company_id )a group by 3,7
5	event_scheduler	localhost	42602	NULL	NULL
